import {Switch} from "@lib/components/ui/switch";
import { Meta } from "@storybook/react";

const meta: Meta<typeof Switch> = {
  component: Switch,
};

export default meta;


export const BaseStory = () => (<Switch/>)
