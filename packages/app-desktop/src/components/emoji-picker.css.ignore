@tailwind base;
@tailwind components;
@tailwind utilities;
@config "../../tailwind.config.cjs"
/* we force the emoji picker to look as if its following the theme */
em-emoji-picker {
  --rgb-background: 0, 0, 0, 0;
  --shadow: none;
}
